# 📚 Руководство по использованию промптов для агента

## 🎯 Назначение

Эти промпты созданы для AI агента, который будет реализовывать **Payment Consent Flow** - первый этап плана доработки для хакатона.

---

## 📁 Структура файлов

### 1. `AGENT_INSTRUCTIONS_PAYMENT_CONSENT.md` ⭐ **ОСНОВНОЙ ФАЙЛ**
**Использование**: Главный промпт с четкими инструкциями

**Содержит**:
- Контекст задачи
- 4 задачи в порядке выполнения
- Референсы на существующий код
- Критерии успеха

**Когда использовать**: Основной файл для передачи агенту. Содержит все необходимое для начала работы.

---

### 2. `AGENT_PROMPT_PAYMENT_CONSENT.md` 📖 **ДЕТАЛЬНЫЙ ПРОМПТ**
**Использование**: Расширенный промпт с примерами кода

**Содержит**:
- Детальное описание каждой задачи
- Примеры кода для каждой задачи
- Тестовые сценарии
- Важные замечания

**Когда использовать**: Если агенту нужны дополнительные детали и примеры.

---

### 3. `PAYMENT_CONSENT_CONTEXT_FILES.md` 📎 **КОНТЕКСТНЫЕ ФАЙЛЫ**
**Использование**: Список файлов для изучения

**Содержит**:
- Список всех файлов с указанием строк
- Паттерны и примеры
- Структуры данных
- Чек-лист реализации

**Когда использовать**: Для понимания структуры проекта и паттернов.

---

## 🚀 Как использовать

### Вариант 1: Минимальный (рекомендуется)

Передай агенту только основной файл:
```
Прочитай файл AGENT_INSTRUCTIONS_PAYMENT_CONSENT.md и реализуй Payment Consent Flow согласно инструкциям.
```

Агент сам найдет нужные файлы в репозитории.

---

### Вариант 2: С контекстом

Передай агенту основной файл + контекстные файлы:
```
Прочитай файлы:
1. AGENT_INSTRUCTIONS_PAYMENT_CONSENT.md - основные инструкции
2. PAYMENT_CONSENT_CONTEXT_FILES.md - список файлов для изучения

Изучи существующий код в:
- hktn/core/obr_client.py (строки 274-360) - пример initiate_product_consent()
- hktn/backend/services/consents.py (строки 77-132) - пример initiate_product_consent()

Реализуй Payment Consent Flow согласно инструкциям.
```

---

### Вариант 3: Максимальный (для сложных случаев)

Передай все три файла:
```
Прочитай все три файла:
1. AGENT_INSTRUCTIONS_PAYMENT_CONSENT.md
2. AGENT_PROMPT_PAYMENT_CONSENT.md  
3. PAYMENT_CONSENT_CONTEXT_FILES.md

Реализуй Payment Consent Flow согласно инструкциям.
```

---

## 📋 Что должен сделать агент

### Задача 1: Добавить метод в `obr_client.py`
- Метод `initiate_payment_consent()`
- Аналогичен `initiate_product_consent()`
- Endpoint: `/payment-consents/request`
- Permissions: `InitiateSinglePayment`, `InitiateMultiplePayment`, `CreateVariableRecurringPayment`

### Задача 2: Добавить функцию в `consents.py`
- Функция `initiate_payment_consent()`
- Сохраняет в БД с `consent_type="payments"`
- Обрабатывает ошибки gracefully

### Задача 3: Обновить `initiate_full_consent_flow()`
- Создает все три типа consent (account + product + payment)
- Product и payment создаются всегда (не только при auto-approve)

### Задача 4: Добавить endpoint
- `POST /api/consent/initiate/payment`

---

## ✅ Проверка результата

После реализации агент должен:

1. **Создать payment consent**:
```bash
curl -X POST http://localhost:8000/api/consent/initiate/payment \
  -H "Content-Type: application/json" \
  -d '{"user_id": "team260-3", "bank_id": "abank"}'
```

2. **Проверить в БД**:
```bash
sqlite3 finpulse_consents.db \
  "SELECT * FROM consents WHERE consent_type='payments';"
```

3. **Проверить full flow**:
```bash
curl -X POST http://localhost:8000/api/consent/initiate \
  -H "Content-Type: application/json" \
  -d '{"user_id": "team260-3", "bank_id": "abank"}'
```

**Ожидаемое поведение**: Создаются все три типа consent.

---

## 🎓 Советы для агента

1. **Изучи существующий код**: `initiate_product_consent()` - это лучший референс
2. **Следуй паттернам**: Используй те же паттерны, что и в существующем коде
3. **Логируй все**: Добавляй логирование для дебага
4. **Обрабатывай ошибки**: Graceful degradation - если payment consent не создается, это не критично
5. **Тестируй**: После реализации проверь все три теста

---

## 📊 Прогресс

После успешной реализации:
- ✅ Payment Consent Flow реализован
- ✅ Все три типа consent создаются автоматически
- ✅ Endpoint для payment consent работает
- ✅ Готово к следующему этапу (объединенный экран банков + согласий)

---

**Готово к использованию! Передай промпт агенту и начни реализацию! 🚀**

