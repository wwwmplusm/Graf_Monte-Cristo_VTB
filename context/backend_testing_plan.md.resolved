# 🧪 План тестирования Backend: Интеграция с банковским API

## Цель
Проверить корректность получения данных из Open Banking Russia API всех доступных банков (А-Банк, В-Банк, С-Банк).

---

## 📋 Чек-лист тестирования

### Этап 0: Подготовка (5 минут)
- [ ] Backend запущен и доступен
- [ ] Есть credentials для всех банков
- [ ] Логирование включено
- [ ] База данных инициализирована

### Этап 1: Базовые эндпоинты (5 минут)
- [ ] Login работает
- [ ] Banks list возвращает 3 банка
- [ ] Каждый банк имеет корректный config

### Этап 2: Consent Flow (15 минут)
- [ ] А-Банк: auto-approve consent
- [ ] В-Банк: manual approve consent (если есть)
- [ ] С-Банк: auto-approve consent
- [ ] Consents сохраняются в БД

### Этап 3: Data Fetching (20 минут)
- [ ] Получение Accounts из каждого банка
- [ ] Получение Balances из каждого банка
- [ ] Получение Transactions из каждого банка
- [ ] Получение Product Agreements из каждого банка
- [ ] Pagination работает для транзакций

### Этап 4: Analytics (10 минут)
- [ ] Dashboard возвращает агрегированные данные
- [ ] Integration Status показывает детали по каждому банку
- [ ] Bootstrap работает для каждого банка

### Этап 5: Error Handling (10 минут)
- [ ] Graceful degradation при недоступности банка
- [ ] Retry логика работает
- [ ] Ошибки логируются корректно

---

## 🚀 ЭТАП 0: Подготовка

### Шаг 0.1: Проверка credentials

**Команда**:
```bash
cd /home/kesha/MyCode/HACKATHON/CASH_PREDICT/hktn

# Проверяем .env файл
cat .env
```

**Ожидаемый результат**:
```env
TEAM_CLIENT_ID=team260
TEAM_CLIENT_SECRET=your_secret_here

ABANK_API_URL=https://abank.open.bankingapi.ru
VBANK_API_URL=https://vbank.open.bankingapi.ru
SBANK_API_URL=https://sbank.open.bankingapi.ru
```

**Проблемы**:
- ❌ Если .env не найден → создать
- ❌ Если credentials пустые → взять из документации хакатона

---

### Шаг 0.2: Запуск бэкенда

**Команда**:
```bash
# В отдельном терминале
cd /home/kesha/MyCode/HACKATHON/CASH_PREDICT/hktn

# Установка зависимостей (если нужно)
pip install -r requirements.txt

# Запуск с логированием
uvicorn backend.app:app --reload --port 8000 --log-level debug
```

**Ожидаемый результат**:
```
INFO:     Uvicorn running on http://127.0.0.1:8000
INFO:     finpulse.backend: Bootstrapping FinPulse backend
INFO:     Application startup complete
```

**Проблемы**:
- ❌ Import error → проверить PYTHONPATH
- ❌ Database error → проверить [init_db()](file:///home/kesha/MyCode/HACKATHON/CASH_PREDICT/hktn/core/database.py#36-139)
- ❌ Port busy → использовать другой порт

---

### Шаг 0.3: Проверка здоровья

**Команда**:
```bash
# В новом терминале
curl http://localhost:8000/docs
```

**Ожидаемый результат**: Swagger UI открывается (HTML страница)

---

## 🔐 ЭТАП 1: Базовые эндпоинты

### Тест 1.1: Login

**Команда**:
```bash
curl -X POST http://localhost:8000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": "team260-test-1",
    "user_name": "Тестовый Пользователь"
  }' | jq
```

**Ожидаемый результат**:
```json
{
  "token": "mock-token-for-team260-test-1",
  "profile": {
    "user_id": "team260-test-1",
    "name": "Тестовый Пользователь",
    "subscription_plan": "free",
    "is_active": true
  }
}
```

**✅ Успех**: Статус 200, токен и профиль возвращены
**❌ Ошибка**: Статус 500 → проверить логи

---

### Тест 1.2: Banks List

**Команда**:
```bash
curl http://localhost:8000/api/banks | jq
```

**Ожидаемый результат**:
```json
{
  "banks": [
    {
      "id": "abank",
      "name": "А-Банк",
      "display_name": "А-Банк",
      "status": "available"
    },
    {
      "id": "vbank",
      "name": "В-Банк",
      "display_name": "В-Банк",
      "status": "available"
    },
    {
      "id": "sbank",
      "name": "С-Банк",
      "display_name": "С-Банк",
      "status": "available"
    }
  ]
}
```

**✅ Успех**: 3 банка в списке
**❌ Ошибка**: Пустой список → проверить [config.py](file:///home/kesha/MyCode/HACKATHON/CASH_PREDICT/hktn/backend/config.py)

---

### Тест 1.3: Bank Config

**Команда**:
```bash
# Проверяем что у каждого банка есть API URL
curl http://localhost:8000/api/banks | jq '.banks[] | {id, status}'
```

**Ожидаемый результат**:
```json
{"id": "abank", "status": "available"}
{"id": "vbank", "status": "available"}
{"id": "sbank", "status": "available"}
```

---

## 🔑 ЭТАП 2: Consent Flow

### Тест 2.1: А-Банк - Auto Approve

**Команда**:
```bash
curl -X POST http://localhost:8000/api/consent/initiate \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": "team260-test-1",
    "bank_id": "abank",
    "user_name": "Тестовый Пользователь"
  }' | jq
```

**Ожидаемый результат (если auto-approve)**:
```json
{
  "consent_id": "consent-abc-123",
  "status": "Authorized",
  "auto_approved": true,
  "approval_url": null,
  "request_id": null,
  "bank_id": "abank"
}
```

**Ожидаемый результат (если manual approve)**:
```json
{
  "consent_id": null,
  "status": "AwaitingAuthorization",
  "auto_approved": false,
  "approval_url": "https://abank.open.bankingapi.ru/approve?request_id=req-xyz",
  "request_id": "req-xyz",
  "bank_id": "abank"
}
```

**Проверка в логах**:
```
INFO: Initiating consent for user 'team260-test-1' at https://abank.open.bankingapi.ru
INFO: Consent initiated with consent_id=... request_id=... (status=Authorized)
```

**✅ Успех**: 
- Статус 200
- `consent_id` или [request_id](file:///home/kesha/MyCode/HACKATHON/CASH_PREDICT/hktn/core/database.py#195-204) присутствует
- Consent сохранён в БД

**❌ Ошибки**:
- `401 Unauthorized` → проверить team credentials
- `403 Forbidden` → проверить client_id/secret
- `500 Internal` → проверить логи backend
- Timeout → проверить доступность банка

**Сохраняем consent_id для следующих тестов**:
```bash
# Если auto-approved
ABANK_CONSENT_ID="consent-abc-123"

# Если manual - нужно пройти по approval_url
```

---

### Тест 2.2: В-Банк - Consent

**Команда**:
```bash
curl -X POST http://localhost:8000/api/consent/initiate \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": "team260-test-1",
    "bank_id": "vbank",
    "user_name": "Тестовый Пользователь"
  }' | jq
```

**Сохраняем**:
```bash
VBANK_CONSENT_ID="..."  # или VBANK_REQUEST_ID
```

---

### Тест 2.3: С-Банк - Consent

**Команда**:
```bash
curl -X POST http://localhost:8000/api/consent/initiate \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": "team260-test-1",
    "bank_id": "sbank",
    "user_name": "Тестовый Пользователь"
  }' | jq
```

**Сохраняем**:
```bash
SBANK_CONSENT_ID="..."
```

---

### Тест 2.4: Проверка в БД

**Команда**:
```bash
# Проверяем что consents сохранились
sqlite3 /home/kesha/MyCode/HACKATHON/CASH_PREDICT/hktn/finpulse.db \
  "SELECT user_id, bank_id, consent_id, status FROM consents WHERE user_id='team260-test-1';"
```

**Ожидаемый результат**:
```
team260-test-1|abank|consent-abc-123|approved
team260-test-1|vbank|consent-xyz-456|approved
team260-test-1|sbank|consent-def-789|approved
```

---

## 📊 ЭТАП 3: Data Fetching

### Тест 3.1: А-Банк - Accounts

**Команда**:
```bash
# Используем consent_id из Теста 2.1
curl "http://localhost:8000/api/banks/abank/bootstrap?user_id=team260-test-1" | jq .accounts
```

**Альтернативно (прямой вызов к банку через OBR client)**:
```python
# В Python shell
from hktn.backend.services.banking import bank_client
import asyncio

async def test():
    async with bank_client("abank") as client:
        accounts = await client.fetch_accounts_with_consent(
            user_id="team260-test-1",
            consent_id="consent-abc-123"
        )
        print(f"Accounts: {len(accounts)}")
        for acc in accounts:
            print(acc)

asyncio.run(test())
```

**Ожидаемый результат**:
```json
[
  {
    "accountId": "acc-001",
    "currency": "RUB",
    "accountType": "Personal",
    "accountSubType": "CurrentAccount",
    "status": "Enabled"
  },
  {
    "accountId": "acc-002",
    "currency": "RUB",
    "accountType": "Personal",
    "accountSubType": "Savings",
    "status": "Enabled"
  }
]
```

**✅ Успех**: Массив accounts с > 0 элементов
**❌ Ошибка**: 
- Пустой массив → возможно у пользователя нет счетов
- 401/403 → проверить consent_id
- 404 → проверить user_id

---

### Тест 3.2: А-Банк - Balances

**Команда**:
```bash
curl "http://localhost:8000/api/banks/abank/bootstrap?user_id=team260-test-1" | jq .balances
```

**Ожидаемый результат**:
```json
[
  {
    "accountId": "acc-001",
    "type": "InterimAvailable",
    "creditDebitIndicator": "Credit",
    "amount": {
      "amount": "45000.50",
      "currency": "RUB"
    },
    "dateTime": "2025-11-20T00:00:00Z"
  }
]
```

**Проверка парсинга**:
```bash
# Должен быть рассчитан total_balance
curl "http://localhost:8000/api/banks/abank/bootstrap?user_id=team260-test-1" | jq '.balances | map(.amount.amount | tonumber) | add'
```

**✅ Успех**: Сумма балансов > 0
**❌ Ошибка**: null или 0 → проверить логику [_sum_balance_amounts()](file:///home/kesha/MyCode/HACKATHON/CASH_PREDICT/hktn/backend/services/banking.py#103-110)

---

### Тест 3.3: А-Банк - Transactions

**Команда**:
```python
# Python test (т.к. транзакции не в bootstrap)
from hktn.backend.services.banking import bank_client
import asyncio

async def test_transactions():
    async with bank_client("abank") as client:
        transactions = await client.fetch_transactions_with_consent(
            user_id="team260-test-1",
            consent_id="YOUR_CONSENT_ID"  # из Теста 2.1
        )
        print(f"Total transactions: {len(transactions)}")
        
        # Проверяем первые 5
        for tx in transactions[:5]:
            print(f"- {tx.booking_date_time}: {tx.amount} {tx.currency} ({tx.credit_debit_indicator})")
            print(f"  Info: {tx.transaction_information}")

asyncio.run(test_transactions())
```

**Ожидаемый результат**:
```
Total transactions: 127
- 2025-11-19T14:30:00Z: 5000.0 RUB (Debit)
  Info: Оплата кредита Банк А
- 2025-11-18T09:15:00Z: 85000.0 RUB (Credit)
  Info: Зарплата ООО "Компания"
...
```

**Проверка pagination**:
```
# В логах должно быть:
INFO: Pagination checkpoint account=acc-001 page=1 next=/accounts/acc-001/transactions?page=2
INFO: Pagination checkpoint account=acc-001 page=2 next=None
```

**✅ Успех**: 
- Транзакций > 0
- Pagination работает (если > 100 транзакций)
- Все поля заполнены корректно

**❌ Ошибка**:
- 0 транзакций → возможно у пользователя новый счёт
- Ошибка парсинга → проверить `_to_transaction_model()`

---

### Тест 3.4: А-Банк - Product Agreements (кредиты)

**Команда**:
```bash
curl "http://localhost:8000/api/banks/abank/bootstrap?user_id=team260-test-1" | jq .credits
```

**Ожидаемый результат**:
```json
[
  {
    "agreementId": "loan-001",
    "productType": "loan",
    "status": "active",
    "currency": "RUB",
    "amount": 450000.0,
    "interestRate": 12.9,
    "monthlyPayment": 9500.0
  }
]
```

**✅ Успех**: Массив кредитов (может быть пустым если нет)
**❌ Ошибка**: 
- 403 → нужен отдельный product consent
- null → проверить fallback на `/credits`

---

### Тест 3.5: Повторить для В-Банк и С-Банк

**Команды**:
```bash
# В-Банк
curl "http://localhost:8000/api/banks/vbank/bootstrap?user_id=team260-test-1" | jq

# С-Банк
curl "http://localhost:8000/api/banks/sbank/bootstrap?user_id=team260-test-1" | jq
```

**Проверяем для каждого**:
- [ ] Accounts получены
- [ ] Balances получены
- [ ] Credits получены (или пустой массив)

---

## 📈 ЭТАП 4: Analytics

### Тест 4.1: Dashboard

**Команда**:
```bash
curl "http://localhost:8000/api/dashboard?user_id=team260-test-1" | jq
```

**Ожидаемый результат**:
```json
{
  "total_balance": 123456.78,
  "bank_statuses": [
    {
      "bank_id": "abank",
      "bank_name": "А-Банк",
      "status": "ok",
      "fetched_at": "2025-11-20T01:00:00"
    },
    {
      "bank_id": "vbank",
      "bank_name": "В-Банк",
      "status": "ok",
      "fetched_at": "2025-11-20T01:00:00"
    }
  ],
  "safe_to_spend_daily": 1250.50,
  "salary_amount": 0,
  "next_salary_date": null,
  "days_until_next_salary": null,
  "upcoming_credit_payment": null
}
```

**Проверки**:
- ✅ `total_balance` = сумма балансов из всех банков
- ✅ `bank_statuses` содержит все подключённые банки
- ⚠️ `safe_to_spend_daily` = 0 (т.к. нет manual inputs)

---

### Тест 4.2: Integration Status

**Команда**:
```bash
curl "http://localhost:8000/api/integration-status?user_id=team260-test-1" | jq
```

**Ожидаемый результат**:
```json
{
  "user_id": "team260-test-1",
  "base_score": {
    "status": "ok",
    "value": 123456.78,
    "currency": "RUB",
    "reason": "Расчёт основан на доступных RUB остатках..."
  },
  "banks": [
    {
      "bank_id": "abank",
      "bank_name": "А-Банк",
      "pipeline_status": "ok",
      "steps": [
        {
          "name": "consent",
          "status": "ok",
          "details": "Consent consent-abc-123 (accounts) утверждён."
        },
        {
          "name": "accounts",
          "status": "ok",
          "details": "Запрос `/accounts` → получено 3 записей."
        },
        {
          "name": "balances",
          "status": "ok",
          "details": "Найдено 3 записей, итого 45000.0 RUB."
        },
        {
          "name": "products",
          "status": "ok",
          "details": "Получено 2 записей."
        }
      ],
      "raw_metrics": {
        "sum_account_balances": 45000.0,
        "sum_credit_debts": 150000.0,
        "used_in_base_score": true
      }
    }
  ]
}
```

**Проверки**:
- ✅ Все steps имеют status "ok"
- ✅ `base_score.value` корректный
- ✅ `sum_account_balances` совпадает с балансами из Теста 3.2

**❌ Если status = "error"**:
```json
{
  "name": "balances",
  "status": "error",
  "details": "HTTPStatusError: 500 Internal Server Error",
  "error_code": "FETCH_ERROR"
}
```
→ Проверить логи, возможно банк недоступен

---

## ⚠️ ЭТАП 5: Error Handling

### Тест 5.1: Недоступный банк

**Подготовка**: Временно изменить URL банка на несуществующий

```python
# В config.py или .env
ABANK_API_URL=https://fake-bank-url.invalid
```

**Команда**:
```bash
# Перезапустить backend
# Затем:
curl "http://localhost:8000/api/integration-status?user_id=team260-test-1" | jq '.banks[] | select(.bank_id=="abank")'
```

**Ожидаемый результат**:
```json
{
  "bank_id": "abank",
  "pipeline_status": "error",
  "steps": [
    {
      "name": "consent",
      "status": "error",
      "details": "Connection error: ...",
      "error_code": "CONNECTION_ERROR"
    }
  ]
}
```

**✅ Успех**: 
- Приложение **не падает**
- Другие банки продолжают работать
- В логах есть WARNING

**❌ Ошибка**: 500 Internal Server Error → graceful degradation не работает

---

### Тест 5.2: Retry логика

**Команда**: Временно добавить задержку в банковский API (mock)

```python
# В obr_client.py, метод _get_bank_token
async def _get_bank_token(self):
    # Эмуляция timeout
    await asyncio.sleep(10)  # искусственная задержка
    ...
```

**Ожидаемое поведение**:
```
# В логах:
WARNING: Request to https://abank.../auth/bank-token timed out, retrying (1/3)
WARNING: Request to https://abank.../auth/bank-token timed out, retrying (2/3)
INFO: Successfully obtained and validated new bank token
```

**✅ Успех**: Retry происходит 3 раза с backoff
**❌ Ошибка**: Сразу падает → проверить `@api_retry` decorator

---

### Тест 5.3: Некорректный consent_id

**Команда**:
```bash
# Используем несуществующий consent
curl "http://localhost:8000/api/banks/abank/bootstrap?user_id=team260-test-1" \
  -H "X-Consent-Override: fake-consent-id" | jq
```

**Ожидаемый результат**:
```json
{
  "detail": "No approved consent found for user team260-test-1 and bank abank"
}
```

**✅ Успех**: Статус 404 или 400, понятное сообщение
**❌ Ошибка**: 500 Internal → плохая обработка ошибок

---

## 📝 ЭТАП 6: Итоговый отчёт

### Результаты тестирования

Заполните таблицу после прохождения всех тестов:

| Тест | Банк | Статус | Проблемы | Примечания |
|------|------|--------|----------|------------|
| Login | - | ✅/❌ | | |
| Banks List | - | ✅/❌ | | |
| Consent Initiate | А-Банк | ✅/❌ | | Auto/Manual approve |
| Consent Initiate | В-Банк | ✅/❌ | | Auto/Manual approve |
| Consent Initiate | С-Банк | ✅/❌ | | Auto/Manual approve |
| Accounts | А-Банк | ✅/❌ | | Кол-во: ___ |
| Accounts | В-Банк | ✅/❌ | | Кол-во: ___ |
| Accounts | С-Банк | ✅/❌ | | Кол-во: ___ |
| Balances | А-Банк | ✅/❌ | | Сумма: ___ |
| Balances | В-Банк | ✅/❌ | | Сумма: ___ |
| Balances | С-Банк | ✅/❌ | | Сумма: ___ |
| Transactions | А-Банк | ✅/❌ | | Кол-во: ___ |
| Transactions | В-Банк | ✅/❌ | | Кол-во: ___ |
| Transactions | С-Банк | ✅/❌ | | Кол-во: ___ |
| Product Agreements | А-Банк | ✅/❌ | | Кол-во: ___ |
| Product Agreements | В-Банк | ✅/❌ | | Кол-во: ___ |
| Product Agreements | С-Банк | ✅/❌ | | Кол-во: ___ |
| Dashboard | - | ✅/❌ | | total_balance: ___ |
| Integration Status | - | ✅/❌ | | base_score: ___ |
| Error Handling | - | ✅/❌ | | Graceful degradation |
| Retry Logic | - | ✅/❌ | | Max retries: 3 |

---

## 🔍 Типичные проблемы и решения

### Проблема 1: 401 Unauthorized

**Причина**: Неверные credentials
**Решение**:
```bash
# Проверить .env
cat .env | grep TEAM_CLIENT

# Проверить что credentials правильные
# Взять из документации хакатона
```

---

### Проблема 2: Пустой массив accounts

**Причина**: 
- У пользователя нет счетов
- Неправильный user_id

**Решение**:
```bash
# Использовать тестовый user_id из песочницы
# Например: team260-demo-user-1
```

---

### Проблема 3: Timeout при запросе транзакций

**Причина**: Много транзакций, долгая pagination

**Решение**:
```python
# Увеличить timeout в httpx client
# В obr_client.py:
self._client = httpx.AsyncClient(
    base_url=api_base_url,
    timeout=httpx.Timeout(60.0)  # увеличить с 30 до 60
)
```

---

### Проблема 4: JWT validation failed

**Причина**: 
- Банк использует HS256 без shared secret
- JWKS недоступен

**Решение**:
```python
# Fallback на decode без проверки signature
# В obr_client.py._validate_jwt():
except Exception as e:
    logger.warning("JWT validation failed: %s, decoding without verification", e)
    return jwt.decode(token, options={"verify_signature": False})
```

---

## 📊 Метрики успешности

**Отлично (90%+)**:
- ✅ Все 3 банка подключены
- ✅ Все типы данных получены (accounts, balances, transactions, agreements)
- ✅ Pagination работает
- ✅ Graceful degradation работает
- ✅ Retry логика работает

**Хорошо (70-89%)**:
- ✅ 2-3 банка подключены
- ✅ Большинство данных получено
- ⚠️ Некоторые edge cases не обработаны

**Требует доработки (<70%)**:
- ❌ < 2 банков работают
- ❌ Критические ошибки в получении данных
- ❌ Нет обработки ошибок

---

## 🎯 Следующие шаги после тестирования

1. **Если все тесты ✅**: Переходим к реализации алгоритмов
2. **Если есть ❌**: Фиксим проблемы в OBR Client
3. **Если данных мало**: Переходим на реальные test users из песочницы

---

Готов помочь с запуском тестов! Начинаем?
