# 📊 Анализ реализованного функционала FinPulse

**Дата анализа:** 20 ноября 2025  
**Репозиторий:** /home/kesha/MyCode/HACKATHON/CASH_PREDICT

---

## 🎯 Краткое резюме

**Процент реализации:** ~70% от задуманного функционала

**Статус:** Работающий MVP с основными фичами, но требующий доработки критических элементов согласно спецификации.

---

## ✅ Реализованный функционал

### 1. **Онбординг (90% реализовано)**

#### ✅ Что работает:
- **Step 1: User Info** - ввод имени пользователя (team260-X)
- **Step 2: Banks & Consents** - выбор банков и матрица согласий
  - Визуальный список банков (VBank, ABank, SBank)
  - Чекбоксы для выбора типов согласий (accounts, products, payments)
- **Step 3: Consent Progress** - создание согласий через OpenBanking API
  - Автоматическое создание account consents
  - Автоматическое создание product consents
  - Автоматическое создание payment consents
  - Отображение статуса подключения банков
- **Step 5: Goal Selection** - выбор цели (закрыть долги / накопить деньги)
  - Определение режима работы (loans / deposits)
  - Выбор скорости погашения (консервативно / сбалансировано / агрессивно)
- **Step 6: Summary** - сводка выбранных параметров
- **Step 7: Loading** - загрузка и анализ данных

#### ❌ Не реализовано (согласно спецификации):
- **Auto-approve check**: После запроса consent не проверяется наличие auto-approve
- **Ссылка на банк для ручного подтверждения**: Если нет auto-approve, не выводится ссылка на банк
- **Повторная проверка статуса**: Нет polling механизма для проверки статуса consent
- **Запрос products**: Не запрашиваются продукты банков через `/products` endpoint
- **Установка repayment_speed**: На Step 5 не сохраняется выбранная скорость погашения в БД

---

### 2. **Backend API (85% реализовано)**

#### ✅ Реализованные endpoints:

**Auth:**
- ❌ Отсутствует (не требуется для хакатона)

**Consents:**
- ✅ `POST /api/consent/initiate` - создание account consent
- ✅ `POST /api/consent/initiate/product` - создание product consent
- ✅ `POST /api/consent/initiate/payment` - создание payment consent
- ✅ `GET /api/consent/status` - проверка статуса consent
- ✅ `GET /api/consent/callback` - callback после подтверждения

**Banks:**
- ✅ `GET /api/banks` - список доступных банков

**Analytics:**
- ✅ `GET /api/dashboard` - главный dashboard с метриками
  - Кеширование на 15-30 минут
  - Force refresh через query parameter
- ✅ `GET /api/integration-status` - статус интеграции с банками

**Payments:**
- ✅ `POST /api/payments/mdp` - обязательный платеж (MDP)
- ✅ `POST /api/payments/adp` - дополнительный платеж (ADP)
- ✅ `POST /api/payments/sdp` - пополнение накоплений (SDP)

**Onboarding:**
- ✅ `POST /api/onboarding/save-financial-inputs` - сохранение финансовых данных

#### ❌ Не реализованные endpoints (согласно спецификации):

**Refinancing:**
- ❌ `GET /api/refinance/optimize-loans` - оптимизация кредитов
- ❌ `GET /api/refinance/deposit-offers` - предложения по вкладам
- ❌ `POST /api/refinance/apply` - подача заявки на рефинансирование

**Loans:**
- ❌ `GET /api/loans` - список всех кредитов с деталями
- ❌ `GET /api/loans/{loan_id}` - детали конкретного кредита
- ❌ `GET /api/loans/simulation` - симулятор закрытия кредитов

**Deposits:**
- ❌ `GET /api/deposits` - список всех вкладов
- ❌ `GET /api/deposits/{deposit_id}` - детали конкретного вклада

**Profile:**
- ❌ `GET /api/profile` - профиль пользователя
- ❌ `POST /api/subscription/upgrade` - переход на Premium

---

### 3. **Алгоритмы расчета (100% реализовано)**

#### ✅ Реализовано согласно спецификации:

**1. transactions_categorization_salary_and_loans**
- ✅ Категоризация транзакций (Credit/Debit)
- ✅ Определение зарплатных поступлений по keywords
- ✅ Расчет среднемесячного дохода (медиана)
- ✅ Определение регулярности дохода (regular_monthly/biweekly/irregular)
- ✅ Прогноз следующей зарплаты
- ✅ Матчинг платежей по кредитам с транзакциями
- ✅ Определение статуса оплаты (paid_in_current_period)

**2. total_debit_balance_calculation**
- ✅ Фильтрация дебетовых счетов (Checking, CurrentAccount, Savings)
- ✅ Исключение кредитных карт и займов
- ✅ Расчет InterimAvailable баланса
- ✅ Конвертация валют в RUB
- ✅ Защита от отрицательных балансов

**3. total_debt_calculation**
- ✅ Фильтрация активных кредитов
- ✅ Гибкий парсинг типов продуктов (loan, credit_card, mortgage)
- ✅ Waterfall поиск полей (amount, currentBalance, outstandingBalance)
- ✅ Суммирование по типам (loans, cards)
- ✅ Конвертация валют

**4. mdp_calculation**
- ✅ Определение планового платежа (из графика или эвристика)
- ✅ Расчет даты платежа
- ✅ Проверка статуса оплаты
- ✅ Расчет дневного платежа (remaining / days_left)
- ✅ Детализация по кредитам

**5. adp_calculation**
- ✅ Расчет по коэффициентам скорости (conservative/balanced/fast)
- ✅ Cap check (max 20% дохода на досрочку)
- ✅ Ранжирование кредитов по стратегии (Avalanche/Snowball)
- ✅ Выбор целевого кредита
- ✅ Защита от переплаты (adp <= loan balance)

**6. sts_calculation**
- ✅ Monte-Carlo симуляция на 30 дней
- ✅ Учет всех списаний по кредитам
- ✅ Учет регулярных доходов (salary)
- ✅ Расчет минимальной точки баланса
- ✅ Вычитание safety buffer и ADP
- ✅ Расчет дневного лимита

**7. loan_ranking_engine (частично реализовано внутри ADP)**
- ✅ Avalanche сортировка (по ставке DESC)
- ✅ Snowball сортировка (по сумме ASC)
- ❌ Нет отдельного endpoint для ранжирования
- ❌ Не сохраняются refi_candidates

**8. financing_need_detector (не реализовано)**
- ❌ Расчет DTI (Debt-to-Income)
- ❌ Сбор триггеров (overdue, gap_risk, high_dti, refi_opportunity)
- ❌ Оценка срочности (HIGH/MEDIUM/WATCH/NONE)

**9. best_financing_offer_selector (не реализовано)**
- ❌ Сценарий "Точечный выстрел" (Single Refi)
- ❌ Сценарий "Консолидация" (All-in)
- ❌ Сортировка по monthly_saving
- ❌ Расчет PMT (аннуитет)

---

### 4. **Frontend Screens (75% реализовано)**

#### ✅ Реализованные экраны:

**OnboardingScreen**
- ✅ Полный flow с 7 шагами
- ✅ Интеграция с Backend API
- ✅ Stepper компонент
- ✅ Валидация на каждом шаге

**HomeScreen**
- ✅ Загрузка dashboard через API
- ✅ Кеширование и force refresh
- ✅ Data freshness indicator
- ✅ Отображение всех виджетов

**LoansDetailScreen**
- ✅ Общий долг
- ✅ MDP/ADP виджеты
- ✅ Список всех кредитов
- ✅ Стратегия погашения (отображение)
- ❌ Нет реальных данных из API (используются mock)

**DepositsDetailScreen**
- ✅ Общие накопления
- ✅ SDP виджет
- ✅ Список вкладов
- ❌ Нет реальных данных из API

**RefinanceScreen**
- ✅ Список офферов рефинансирования
- ✅ Список офферов по вкладам
- ✅ Выбор оффера и подача заявки
- ❌ Данные mock (нет реального API)

**STSDetailScreen**
- ✅ Детальная информация о STS
- ✅ График на 30 дней
- ✅ Tomorrow impact

**CardsScreen**
- ✅ Список дебетовых карт
- ✅ Общий баланс
- ❌ Нет группировки по банкам

**ProfileScreen (не реализован)**
- ❌ Отсутствует
- ❌ Нет подключенных банков
- ❌ Нет управления подпиской

---

### 5. **Widgets (100% реализовано для MVP)**

#### ✅ Реализованные виджеты:

**HealthWidget**
- ✅ Отображение health score (0-100)
- ✅ Статус (excellent/good/fair/poor)
- ✅ Цветовое кодирование
- ✅ Reasons (список причин)

**STSWidget**
- ✅ Сумма Safe-to-Spend на сегодня
- ✅ Прогресс-бар (spent / total)
- ✅ Tomorrow impact
- ✅ Навигация на детальный экран

**LoansDepositsWidget**
- ✅ Переключение режима (loans/deposits)
- ✅ Отображение total_outstanding / total_saved
- ✅ MDP/ADP/SDP кнопки
- ✅ Навигация на детальный экран

**DebitCardsWidget**
- ✅ Общий баланс по всем картам
- ✅ Список карт с балансами
- ✅ Навигация на детальный экран

**UpcomingEventsWidget**
- ✅ Топ-3 события из events_next_30d
- ✅ Цветовое кодирование по типу
- ✅ Индикация срочности (<3 дней)
- ✅ Quick pay для loan_payment

**RefinanceTriggersWidget**
- ✅ Отображение триггеров оптимизации
- ✅ Urgency levels
- ✅ Savings информация
- ✅ Quick action кнопки

**QuickActionsWidget**
- ✅ Быстрые действия для MDP/ADP/SDP
- ✅ Кнопка рефинансирования
- ✅ Адаптивность под режим (loans/deposits)

**TimelineWidget (частично)**
- ✅ Компонент создан
- ❌ Не используется на главном экране

---

### 6. **Интеграция с OpenBanking API (90% реализовано)**

#### ✅ Реализовано:

**OBR Client (hktn/core/obr_client.py)**
- ✅ Полная реализация OpenBanking Russia v2.1
- ✅ JWKS ключи и JWT подпись
- ✅ OAuth2 авторизация
- ✅ Retry механизм
- ✅ Таймауты

**Endpoints:**
- ✅ `POST /account-consents/request` - создание account consent
- ✅ `POST /account-consents/{request_id}/confirm` - подтверждение consent
- ✅ `GET /accounts` - список счетов
- ✅ `GET /accounts/{account_id}/balances` - балансы
- ✅ `GET /accounts/{account_id}/transactions` - транзакции
- ✅ `GET /product-agreements` - кредитные договоры
- ✅ `POST /product-consents/request` - создание product consent
- ✅ `POST /product-consents/{request_id}/confirm` - подтверждение product consent
- ✅ `POST /payment-consents/request` - создание payment consent
- ✅ `POST /payment-consents/{request_id}/confirm` - подтверждение payment consent
- ✅ `POST /payments/single` - single payment

#### ❌ Не используется:
- ❌ `POST /payments/multiple` - multiple payments
- ❌ `POST /payments/vrp` - variable recurring payments

---

### 7. **Database (100% реализовано)**

#### ✅ SQLite база данных:

**Таблицы:**
- ✅ `consents` - хранение всех согласий
  - Поля: user_id, bank_id, consent_id, consent_type, status, created_at, expires_at, request_id
- ✅ `user_financial_inputs` - финансовые данные пользователя
  - Поля: user_id, salary_amount, next_salary_date, credit_payment_amount, credit_payment_date, savings_target, savings_goal_date, repayment_speed, repayment_strategy
- ✅ `dashboard_cache` - кеш dashboard метрик
  - Поля: user_id, dashboard_data (JSON), calculated_at, expires_at
- ✅ `bank_data_cache` - кеш данных из банков
  - Поля: user_id, bank_id, data_type, data (JSON), fetched_at, expires_at

**Функции:**
- ✅ Инициализация БД при старте
- ✅ CRUD операции для consents
- ✅ Кеширование и инвалидация

---

## ❌ Критические недостатки

### 1. **Нарушения спецификации онбординга**

**Проблема:** Текущий flow не соответствует спецификации (см. "back onboard.md", раздел "## 1. Онбординг, подключение банков и выбор цели")

**Что не так:**

1. **Step 1.5 (Consent processing)**: 
   - ❌ Не проверяется auto-approve
   - ❌ Не отображается ссылка на банк для ручного подтверждения
   - ❌ Нет polling механизма

2. **Step 1.6 (Goal selection)**:
   - ❌ Кнопка "Закрыть кредиты" доступна всегда, а должна быть только если есть кредиты
   - ❌ Не сохраняется repayment_speed в БД

3. **Step 1.9-1.10 (Loading → Home)**:
   - ❌ Нет индикатора прогресса анализа
   - ❌ Нет сообщения "Мы анализируем ваши финансы..."

**Критичность:** 🔴 HIGH  
**Требуется для хакатона:** ДА

---

### 2. **Отсутствие Refinancing API и логики**

**Проблема:** RefinanceScreen использует только mock данные

**Что нужно:**
- ❌ Backend endpoint `/api/refinance/optimize-loans`
- ❌ Реализация алгоритма financing_need_detector
- ❌ Реализация алгоритма best_financing_offer_selector
- ❌ Интеграция с product catalog

**Критичность:** 🟠 MEDIUM  
**Требуется для хакатона:** ДА (это killer feature)

---

### 3. **Отсутствие Profile экрана**

**Проблема:** Нет экрана профиля согласно спецификации

**Что нужно:**
- ❌ Отображение имени пользователя
- ❌ Статус подписки (Free/Premium)
- ❌ Подключенные банки (логотип + статус)
- ❌ Подключенные продукты (кол-во кредитов, вкладов, карт)
- ❌ Кнопка "Перейти на платную версию"

**Критичность:** 🟡 LOW  
**Требуется для хакатона:** НЕТ (nice to have)

---

### 4. **Loans и Deposits Detail используют mock данные**

**Проблема:** Список кредитов и вкладов не загружается из API

**Что нужно:**
- ❌ Backend endpoint `/api/loans`
- ❌ Backend endpoint `/api/deposits`
- ❌ Парсинг product agreements на backend
- ❌ Отображение реальных данных на frontend

**Критичность:** 🟠 MEDIUM  
**Требуется для хакатона:** ДА

---

### 5. **STS.spent всегда 0**

**Проблема:** Не отслеживаются потраченные средства за текущий день

**Что нужно:**
- ❌ Фильтрация транзакций за сегодня (debit)
- ❌ Суммирование сумм
- ❌ Обновление при каждом запросе dashboard

**Критичность:** 🟡 LOW  
**Требуется для хакатона:** НЕТ

---

### 6. **Отсутствие loan/deposit simulation**

**Проблема:** Нет симулятора закрытия кредитов и достижения целей

**Что нужно:**
- ❌ Backend endpoint `/api/loans/simulation`
- ❌ Алгоритм прогнозирования закрытия кредитов
- ❌ UI компонент симулятора
- ❌ Интерактивные слайдеры (изменение ADP → новая дата закрытия)

**Критичность:** 🟡 LOW  
**Требуется для хакатона:** НЕТ (nice to have)

---

## 📋 План доработки (по приоритетам)

### 🔴 КРИТИЧЕСКИЙ ПРИОРИТЕТ (для хакатона)

#### 1. Исправление онбординга (4-6 часов)

**Задачи:**

**Backend:**
- [ ] В `consents.py` добавить проверку auto-approve после создания consent
- [ ] Вернуть в ответе поле `auto_approved: bool`
- [ ] Если `auto_approved=false`, вернуть `authorization_url` для ручного подтверждения

**Frontend:**
- [ ] В `Step2ConsentProgress.tsx` добавить отображение ссылки на банк
- [ ] Добавить polling механизм (каждые 5 сек) проверки статуса consent
- [ ] Только после approve переходить к следующему банку

**Файлы:**
- `hktn/backend/services/consents.py`
- `hktn/src/components/steps/Step2ConsentProgress.tsx`

---

#### 2. Реализация Refinancing API (6-8 часов)

**Задачи:**

**Backend:**
- [ ] Создать `hktn/backend/routers/refinance.py`
- [ ] Реализовать алгоритм `financing_need_detector()` в `algorithms.py`
- [ ] Реализовать алгоритм `best_financing_offer_selector()` в `algorithms.py`
- [ ] Endpoint `GET /api/refinance/optimize-loans`
  - Вход: user_id
  - Выход: top_offers[] (sorted by monthly_saving)
- [ ] Endpoint `POST /api/refinance/apply`
  - Создание заявки (можно mock)
- [ ] Загрузка product catalog из JSON

**Frontend:**
- [ ] В `RefinanceScreen.tsx` заменить mock на API call
- [ ] Отображение реальных офферов
- [ ] Интеграция с `/api/refinance/apply`

**Файлы:**
- `hktn/backend/routers/refinance.py` (создать)
- `hktn/backend/services/algorithms.py`
- `hktn/src/screens/RefinanceScreen.tsx`
- `hktn/team260_data/products/catalog.json`

---

#### 3. Реализация Loans и Deposits Detail API (4-6 часов)

**Задачи:**

**Backend:**
- [ ] Создать `hktn/backend/routers/loans.py`
- [ ] Endpoint `GET /api/loans`
  - Парсинг product-agreements (type=loan)
  - Обогащение данными из debt_obligations_status
  - Добавление priority (из loan_ranking_engine)
- [ ] Endpoint `GET /api/deposits`
  - Парсинг product-agreements (type=deposit)
  - Добавление rate, term, balance

**Frontend:**
- [ ] В `LoansDetailScreen.tsx` загрузка через API
- [ ] В `DepositsDetailScreen.tsx` загрузка через API

**Файлы:**
- `hktn/backend/routers/loans.py` (создать)
- `hktn/src/screens/LoansDetailScreen.tsx`
- `hktn/src/screens/DepositsDetailScreen.tsx`

---

### 🟠 ВЫСОКИЙ ПРИОРИТЕТ (желательно для хакатона)

#### 4. Goal Selection: блокировка "Закрыть кредиты" (1-2 часа)

**Задачи:**

**Frontend:**
- [ ] В `Step5Questions.tsx` загрузить product-agreements через API
- [ ] Проверить наличие кредитов (type=loan или credit_card)
- [ ] Если кредитов нет → кнопка "Закрыть кредиты" disabled + tooltip

**Файлы:**
- `hktn/src/components/steps/Step5Questions.tsx`

---

#### 5. Сохранение repayment_speed (1 час)

**Задачи:**

**Frontend:**
- [ ] В `Step5Questions.tsx` отправлять repayment_speed через `/api/onboarding/save-financial-inputs`

**Backend:**
- [ ] В `onboarding.py` сохранять repayment_speed в БД
- [ ] Использовать в `adp_calculation()`

**Файлы:**
- `hktn/src/components/steps/Step5Questions.tsx`
- `hktn/backend/services/onboarding.py`

---

#### 6. Health Score: добавить reasons (2-3 часа)

**Задачи:**

**Backend:**
- [ ] В `_calculate_health_score()` генерировать список reasons
  - Примеры: "STS выше 40%", "Долг снизился на 5%", "DTI < 50%"
- [ ] Вернуть в `health_score.reasons[]`

**Файлы:**
- `hktn/backend/services/analytics.py`

---

### 🟡 СРЕДНИЙ ПРИОРИТЕТ (nice to have)

#### 7. Profile Screen (3-4 часа)

**Задачи:**

**Backend:**
- [ ] Endpoint `GET /api/profile`
  - user_name, subscription_plan, connected_banks[], products_overview

**Frontend:**
- [ ] Создать `ProfileScreen.tsx`
- [ ] Отображение имени, статуса подписки
- [ ] Список подключенных банков
- [ ] Кнопка "Перейти на платную версию"

**Файлы:**
- `hktn/backend/routers/profile.py` (создать)
- `hktn/src/screens/ProfileScreen.tsx` (создать)

---

#### 8. STS.spent отслеживание (1-2 часа)

**Задачи:**

**Backend:**
- [ ] В `_calculate_dashboard_metrics()` фильтровать транзакции за сегодня
- [ ] Суммировать Debit транзакции
- [ ] Вернуть в `sts_today.spent`

**Файлы:**
- `hktn/backend/services/analytics.py`

---

#### 9. Loan Simulation (4-6 часов)

**Задачи:**

**Backend:**
- [ ] Endpoint `GET /api/loans/simulation`
  - Вход: current_adp, new_adp
  - Выход: projected_payoff_date, total_interest_saved

**Frontend:**
- [ ] В `LoansDetailScreen.tsx` добавить симулятор
- [ ] Интерактивный слайдер для ADP
- [ ] Отображение прогноза

**Файлы:**
- `hktn/backend/routers/loans.py`
- `hktn/src/screens/LoansDetailScreen.tsx`

---

## 📊 Оценка трудозатрат

| Приоритет | Задачи | Время | Критично |
|-----------|--------|-------|----------|
| 🔴 Критический | 1. Онбординг<br>2. Refinancing API<br>3. Loans/Deposits API | 14-20 часов | ДА |
| 🟠 Высокий | 4. Goal Selection<br>5. Repayment Speed<br>6. Health Reasons | 4-6 часов | Желательно |
| 🟡 Средний | 7. Profile Screen<br>8. STS.spent<br>9. Loan Simulation | 8-12 часов | НЕТ |

**Итого для хакатона:** 18-26 часов (только критические и высокоприоритетные задачи)

---

## 🎯 Рекомендации для демонстрации

### Что показать на хакатоне:

✅ **Работает и впечатляет:**
1. **Онбординг** - плавный flow с реальным подключением к банкам
2. **Dashboard** - все виджеты с реальными данными
3. **STS Widget** - главный индикатор "сколько можно потратить"
4. **MDP/ADP платежи** - реальное списание через OpenBanking API
5. **Алгоритмы расчета** - 6 из 9 алгоритмов полностью реализованы

❌ **Нужно доработать:**
1. **Refinancing** - пока mock, нужна реальная логика
2. **Loans Detail** - показывать реальные кредиты из API
3. **Consent flow** - добавить ссылку на банк для ручного подтверждения

---

## 🚀 Стратегия завершения

### День 1 (8 часов):
- [ ] Исправление онбординга (6 часов)
- [ ] Goal Selection блокировка (2 часа)

### День 2 (8 часов):
- [ ] Refinancing API полностью (8 часов)

### День 3 (8 часов):
- [ ] Loans/Deposits Detail API (6 часов)
- [ ] Health Reasons (2 часа)

---

## 📝 Итоговая оценка

**Плюсы:**
- ✅ Сильный backend с качественными алгоритмами
- ✅ Хорошая архитектура и структура кода
- ✅ Реальная интеграция с OpenBanking API
- ✅ Кеширование и оптимизация
- ✅ Красивый и функциональный UI

**Минусы:**
- ❌ Некоторые экраны используют mock данные
- ❌ Отсутствие Refinancing логики
- ❌ Нарушения спецификации в онбординге
- ❌ Нет Profile экрана

**Вердикт:** Проект на 70% готов. При концентрации усилий на критических задачах (18-26 часов работы) можно довести до 95% соответствия спецификации и получить полноценный демонстрационный продукт для хакатона.

